language: python

python:
    - "3.6"

notifications:
  email:
      - mitsuo_h@outlook.com

before_install:
    - curl --version

install:
    - pip install -r requirements.txt

script:
    - ./scripts/docker_hub_rebuild.sh